export const PDF_FILE_SIZE_LIMIT_COPY = "up to 100MB per file";

export const PDF_APP_DESCRIPTION = `Reorder, stitch, rotate, or carve out PDF pages inside the tab—supported work stays local (${PDF_FILE_SIZE_LIMIT_COPY} per file), fair-use safeguards still apply, and no Helvety subscription gates the tools. Swiss-built under the MIT license.`;
