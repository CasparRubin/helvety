import { HeroSection } from "@/components/hero-section";

/** Gateway home (`/`): {@link HeroSection} with Hyperspeed (client-only, fades in after `onReady`) and React Bits hero text. */
export default function Page() {
  return <HeroSection />;
}
