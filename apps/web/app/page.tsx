import { HeroSection } from "@/components/hero-section";

/** Gateway home (`/`): {@link HeroSection}: Hyperspeed WebGL in dark mode only (client-only) and React Bits hero text. */
export default function Page() {
  return <HeroSection />;
}
