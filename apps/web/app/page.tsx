import { HeroSection } from "@/components/hero-section";

/** Gateway home (`/`): {@link HeroSection}: Hyperspeed WebGL (client-only) and React Bits hero text. */
export default function Page() {
  return <HeroSection />;
}
