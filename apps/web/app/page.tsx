import { HeroSection } from "@/components/hero-section";

/** Home page (`/`): client `HeroSection` — viewport-centered copy, dot-grid background, `md`+ identifier column, and hero-only identifier shine (`globals.css` + `HelvetyIdentifier` `edgeHighlight` when motion is allowed). */
export default function Page() {
  return <HeroSection />;
}
