import { HeroMarketingShell } from "@/components/hero-marketing-shell";

/** Gateway home (`/`): server-rendered hero copy with client SideRays backdrop. */
export default function Page() {
  return <HeroMarketingShell />;
}
