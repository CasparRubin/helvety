import { HeroSection } from "@/components/hero-section";

/** Gateway home (`/`): {@link HeroSection}: Hyperspeed WebGL (light and dark, client-only) and React Bits hero text. */
export default function Page() {
  return <HeroSection />;
}
