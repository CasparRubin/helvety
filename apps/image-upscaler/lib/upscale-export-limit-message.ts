/** Shown when canvas / OffscreenCanvas cannot allocate or export at requested size. */
export const UPSCALE_EXPORT_SIZE_LIMIT_MESSAGE =
  "Your browser cannot export at the requested pixel size. Try a lower scale, a smaller target size, or a smaller source image.";
