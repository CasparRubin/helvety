"use client";

import {
  EntityCommandBar,
  type EntityCommandBarProps,
} from "@helvety/ui/entity-command-bar";

/**
 *
 */
type LinksCommandBarProps = EntityCommandBarProps;

/**
 *
 */
export function LinksCommandBar(props: LinksCommandBarProps) {
  return <EntityCommandBar {...props} />;
}
