import { HelvetyImageEditor } from "@/components/helvety-image-editor";

/**
 * Main page - server component
 * No login or account required.
 */
export default function Page(): React.JSX.Element {
  return <HelvetyImageEditor />;
}
