# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from: Supabase Dashboard > Project Settings > API
# Env names match Supabase docs: https://supabase.com/docs/guides/auth/server-side/nextjs

# Project URL - RESTful endpoint for database access
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon Key (publishable) - Safe for browser use with Row Level Security
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here

# =============================================================================
# APP & CROSS-APP URLs
# =============================================================================
# Used for auth redirects, the app-switcher component, and deep links.
# Cross-app URLs are optional with sensible production defaults if not set.
# NOTE: Make sure NEXT_PUBLIC_APP_URL is in your Supabase Redirect URLs allowlist
# (Supabase Dashboard > Authentication > URL Configuration > Redirect URLs)
#
# All apps served via gateway (path-based routing). dev = localhost:3001, prod = helvety.com
#                                 dev                           prod
# helvety.com (web)              http://localhost:3001         https://helvety.com
# auth                           http://localhost:3001/auth   https://helvety.com/auth
# store                          http://localhost:3001/store  https://helvety.com/store
# pdf                            http://localhost:3001/pdf    https://helvety.com/pdf
# tasks                          http://localhost:3001/tasks   https://helvety.com/tasks
# contacts (this app)            http://localhost:3001/contacts https://helvety.com/contacts

NEXT_PUBLIC_APP_URL=http://localhost:3001/contacts
NEXT_PUBLIC_HOME_URL=http://localhost:3001
NEXT_PUBLIC_AUTH_URL=http://localhost:3001/auth
NEXT_PUBLIC_STORE_URL=http://localhost:3001/store
NEXT_PUBLIC_PDF_URL=http://localhost:3001/pdf
NEXT_PUBLIC_TASKS_URL=http://localhost:3001/tasks
NEXT_PUBLIC_CONTACTS_URL=http://localhost:3001/contacts

# Cookie domain (same-origin session sharing; all apps under helvety.com paths, no longer cross-subdomain)
COOKIE_DOMAIN=.helvety.com

# =============================================================================
# UPSTASH REDIS (Rate Limiting)
# =============================================================================
# Get these from: https://console.upstash.com/ > Redis > Your Database > REST API
# Used for distributed rate limiting across serverless invocations.
# In production, rate limiting fails closed (rejects requests) if Redis is unavailable
# or if these credentials are not configured.
# In development, falls back to in-memory rate limiting if not configured.

UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here
