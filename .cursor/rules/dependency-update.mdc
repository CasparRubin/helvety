---
description: >-
  When updating dependencies, bumping packages, or running deps:drift / bun
  outdated, follow the dependency-update skill and extended inventory (not
  npm-only).
alwaysApply: false
---

# Dependency updates

Apply the project skill [`.cursor/skills/dependency-update/SKILL.md`](../skills/dependency-update/SKILL.md) and walk [docs/dependency-inventory.md](../../docs/dependency-inventory.md).

Include non-npm assets (PDF.js worker, Tesseract WASM/assets, git overrides, external repos), not only `package.json`.
